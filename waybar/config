{
    "layer": "top",
    "modules-left": ["sway/workspaces", "sway/mode", "cpu", "memory", "temperature", "network"],
    "modules-center": ["clock"],
    "modules-right": ["idle_inhibitor", "pulseaudio", "battery", "tray"],
    "sway/window": {
        "max-length": 50
    },

    "cpu": {
        "format": " {usage}%",
        "tooltip": false
    },
    "memory": {
        "interval": 2,
        "format": " {used:0.1f} GB"
    },
    "battery": {
        "format": "{icon} {capacity}%",
        "format-icons": ["", "", "", "", ""]
    },
     "temperature": {
        "thermal-zone": 2,
        "hwmon-path": "/sys/class/hwmon/hwmon3/temp1_input",
        "critical-threshold": 80,
        "format-critical": " {temperatureC}°C",
        "format": " {temperatureC}°C"
    },
    "clock": {
        "format": "{:%H:%M - %a, %d. %b}"
    },
    "network": {
        "interface": "wlp3s0",
        "format": "{ifname}",
        "format-wifi": " {essid}  {bandwidthDownOctets}  {bandwidthUpOctets}",
        "format-ethernet": " {ifname}",
        "format-disconnected": "Disconnected", 
        "interval": 3,
        "max-length": 50
    },
    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-bluetooth": "{volume}% {icon}",
        "format-muted": " Muted",
        "format-icons": {
            "default": ["", "", "", "", ""]
        },
        "scroll-step": 5,
        "on-click": "pulsemixer --toggle-mute"
    }
}
